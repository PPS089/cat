# 生产环境特定配置
server:
  port: 8080

spring:
  # 生产环境数据源配置
  datasource:
    url: jdbc:mysql://localhost:3306/pet_project?useSSL=false&serverTimezone=UTC&characterEncoding=utf-8
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:zx4221335}
    driver-class-name: com.mysql.cj.jdbc.Driver
    type: com.alibaba.druid.pool.DruidDataSource
    
  # Redis配置
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:zx4221335}
      database: ${REDIS_DATABASE:0}
      timeout: 5000ms
      lettuce:
        pool:
          max-active: 20
          max-idle: 10
          min-idle: 5
          max-wait: -1ms

# 日志级别
logging:
  level:
    com.example: info
    org.springframework: info
    com.baomidou.mybatisplus: info
    root: warn

# 生产环境特定标志
prod:
  enabled: true